function ready() {
    var counter = 0
    const zero = 0
    const one = 1
    const two = 2
    const three = 3
    const four = 4
    const five = 5
    if (test.q1[one].checked) {counter++;}
    if (test.q2[zero].checked) {counter++;}
    if (test.q3.value == "color") {counter++;}
    if (test.q4[three].checked) {counter++;}
    if (test.q5[zero].checked) {counter++;}
    if (test.q6[one].checked) {counter++;}
    if (test.q7[three].checked) {counter++;}
    if (test.q8[one].checked) {counter++;}
    if (test.q9.value == "img") {counter++;}
    if (test.q10[zero].checked) {counter++;}
    if (test.q11[three].checked) {counter++;}
    if (test.q12[one].checked) {counter++;}
    if (test.q13.value == "caption") {counter++;}
    if (test.q14[three].checked) {counter++;}
    if (test.q15[one].checked) {counter++;}
    if (test.q16[zero].checked) {counter++;}
    if (test.q17[zero].checked) {counter++;}
    if (test.q18[two].checked) {counter++;}
    if (test.q19[one].checked && test.q19[zero].checked && test.q19[two].checked && !test.q19[three].checked) {counter++;}
    if (test.q20[one].checked) {counter++;}
    if (test.q21[zero].checked) {counter++;}
    if (test.q22[one].checked) {counter++;}
    if (test.q23[three].checked) {counter++;}
    if (test.q24[zero].checked) {counter++;}
    if (test.q25[three].checked) {counter++;}
    if (test.q26[zero].checked) {counter++;}
    if (test.q27[three].checked) {counter++;}
    if (test.q28.value=="background") {counter++;}
    if (test.q29[one].checked) {counter++;}
    if (test.q30.value == "width") {counter++;}
    if (test.q31.value == "height") {counter++;}
    if (test.q32[two].checked) {counter++;}
    if (test.q33[one].checked) {counter++;}
    if (test.q34[one].checked) {counter++;}
    if (test.q35[one].checked) {counter++;}
    if (test.q36[three].checked) {counter++;}
    if (test.q37[two].checked) {counter++;}
    if (test.q38[one].checked) {counter++;}
    if (test.q39[two].checked) {counter++;}
    if (test.q40[zero].checked) {counter++;}
    if (test.q41[two].checked) {counter++;}
    if (test.q42[three].checked) {counter++;}
    if (test.q43[three].checked) {counter++;}
    if (test.q44[one].checked) {counter++;}
    if (test.q45[two].checked) {counter++;}
    if (test.q46[one].checked && test.q46[two].checked && test.q46[three].checked && !test.q46[zero].checked && !test.q46[four].checked) {counter++;}
    if (test.q47[two].checked) {counter++;}
    if (test.q48[one].checked) {counter++;}
    if (test.q49[zero].checked && test.q49[one].checked && !test.q49[two].checked && !test.q49[three].checked && !test.q49[four].checked && !test.q49[five].checked) {counter++;}
    if (test.q50[zero].checked) {counter++;}
    if (test.q51[one].checked) {counter++;}
    if (test.q52[zero].checked) {counter++;}
    if (test.q53.value=="padding") {counter++;}
    if (test.q54[zero].checked) {counter++;}
    if (test.q55[one].checked) {counter++;}
    if (test.q56[zero].checked) {counter++;}
    if (test.q57[zero].checked) {counter++;}
    if (test.q58[zero].checked) {counter++;}
    if (test.q59[three].checked) {counter++;}
    if (test.q60.value == "alert()" || test.q60.value == "alert") {counter++;}
    if (test.q61.value == "15") {counter++;}
    if (test.q62[one].checked) {counter++;}
    if (test.q63[three].checked) {counter++;}
    if (test.q64[zero].checked && test.q64[two].checked && !test.q64[three].checked && !test.q64[one].checked) {counter++;}
    if (test.q65[zero].checked) {counter++;}
    if (test.q66[one].checked) {counter++;}
    if (test.q67[one].checked) {counter++;}
    if (test.q68[zero].checked) {counter++;}
    if (test.q69[one].checked) {counter++;}
    if (test.q70[two].checked) {counter++;}
    if (test.q71[three].checked) {counter++;}
    if (test.q72[two].checked) {counter++;}
    if (test.q73[two].checked) {counter++;}
    if (test.q74[one].checked) {counter++;}
    if (test.q75[two].checked) {counter++;}
    if (test.q76[two].checked) {counter++;}
    if (test.q77[two].checked) {counter++;}
    if (test.q78[zero].checked) {counter++;}
    if (test.q79[one].checked) {counter++;}
    if (test.q80[one].checked) {counter++;}
    if (test.q81[three].checked) {counter++;}
    if (test.q82[three].checked) {counter++;}
    if (test.q83.value == "4") {counter++;}
    if (test.q84[one].checked) {counter++;}
    if (test.q85.value == "дупкин") {counter++;}
    if (test.q86[two].checked) {counter++;}
    if (test.q87.value == "null") {counter++;}
    if (test.q88[zero].checked) {counter++;}
    if (test.q89[three].checked) {counter++;}
    if (test.q90[two].checked) {counter++;}
    if (test.q91[zero].checked) {counter++;}
    if (test.q92[zero].checked) {counter++;}
    if (test.q93[two].checked) {counter++;}
    if (test.q94[two].checked) {counter++;}
    if (test.q95[three].checked) {counter++;}
    if (test.q96[zero].checked) {counter++;}
    if (test.q97[zero].checked) {counter++;}
    if (test.q98[three].checked) {counter++;}
    if (test.q99[one].checked) {counter++;}
    if (test.q100[three].checked) {counter++;}
    alert(counter);
};